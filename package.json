{
  "name": "adab-pitch",
  "version": "1.0.0",
  "private": true,
  "description": "AdaB Health — standalone pitch / program-overview dashboard (sample data)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
